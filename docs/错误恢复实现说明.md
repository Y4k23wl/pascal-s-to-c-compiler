# Pascal-S 错误恢复实现说明

## 1. 实现目标

本次实现按照“课程设计级、保守恢复”的原则落地错误恢复：

1. 一次编译尽量继续向后分析并报告多条错误
2. 恢复范围只放在边界清晰的位置
3. 有错误时仍然禁止进入代码生成
4. 避免恢复本身制造大量伪错误或崩溃

对应参考设计文档：

- `docs/错误恢复实现设计.md`

## 2. 实现分阶段

### 2.1 第一阶段：基础语法恢复

优先实现了高价值、低风险的恢复点：

- `statement_list`
  - 增加“语句之间可能缺少分号”的恢复规则
  - 增加“当前语句损坏，跳过到下一个分号或 `end`”的恢复规则
- `compound_statement`
  - 增加 `begin ... end` 的结构级兜底恢复
- `formal_parameter`
  - 形参列表出错时同步到 `)`
- `procedure_call` / `factor`
  - 过程/函数调用实参列表出错时同步到 `)`
- `READ` / `WRITE`
  - 输入输出参数列表出错时同步到 `)`
- `id_varpart`
  - 数组下标列表出错时同步到 `]`

恢复动作的 AST 策略如下：

- 坏语句直接丢弃，不破坏已有 `statement_list`
- 形参列表恢复为 `AST_PARAM_LIST`
- 实参列表恢复为 `AST_EXPRESSION_LIST`
- `read` 恢复为空 `AST_VARIABLE_LIST`
- 复合语句兜底恢复为空 `AST_STATEMENT_LIST`

这样做的原因是：

- 不新增 AST kind，和现有语义/代码生成接口兼容
- `ast_validate()` 仍能通过
- 恢复后节点结构稳定，不依赖大量额外空指针判断

### 2.2 第二阶段：错误计数与停止条件

在词法和语法两层都加入了错误数量上限：

- 词法：`kMaxLexErrors = 20`
- 语法：`kMaxParseErrors = 20`

行为约定：

- 每次报错时累计计数
- 达到上限后输出“错误过多，停止分析”
- 词法层返回 EOF 停止扫描
- 语法层通过全局停止标记通知词法器不再继续取 token

此外，未闭合注释会被视为“词法提前终止”：

- 先报告词法错误
- 直接结束扫描
- 抑制由截断输入额外诱发的伪语法错误

### 2.3 第三阶段：驱动与阶段判定

前端接口新增了阶段状态查询函数：

- `pascal_s_had_lexical_error()`
- `pascal_s_lexical_error_count()`
- `pascal_s_had_syntax_error()`
- `pascal_s_syntax_error_count()`

驱动层据此补充了阶段性总结：

- 词法阶段错误汇总
- 语法阶段错误汇总
- 语义阶段错误汇总

并保持原有总原则不变：

- 词法或语法阶段只要出错，就不进入语义后续生成链路
- 语义阶段出错，也不生成 C

## 3. 关键实现细节

### 3.1 `statement_list` 的取舍

本项目最常见、最值得恢复的错误是语句边界问题，因此主恢复点放在 `statement_list`。

当前采用了两条规则：

- `statement_list error statement`
  - 用于“缺少分号后直接开始下一条语句”
- `statement_list SEMICOLON error`
  - 用于“分号后当前语句本身损坏，跳过到下一同步点”

这两条规则和 Pascal-S 的“空语句”会引入 2 个受控的 `shift/reduce` 冲突。  
为避免误判，语法文件中显式使用了 `%expect 2` 固定该行为。

实际选择是优先走恢复分支，这比简单把它们删掉更符合本项目的展示目标。

### 3.2 为什么不用复杂错误节点

课程设计里不需要工业级恢复。若引入额外 `AST_ERROR_*` 节点，会带来：

- AST 校验逻辑修改
- 语义分析额外分支
- 代码生成额外分支
- 文档和测试范围同步扩大

因此本次实现坚持复用现有节点：

- 空语句
- 空列表
- 已成功构造的前序节点

### 3.3 为什么不带错生成 C

错误恢复的目的是“多报错”，不是“修程序”。  
恢复后的 AST 只保证结构稳定，不保证语义可信，因此：

- 前端出错后统一返回 `nullptr`
- 驱动层停止后续生成

这可以避免课程设计演示时出现“明明前面报了一堆错，后面还生成一份看似正常的 C”的歧义。

## 4. 涉及文件

- `pascal_s_parser.y`
  - 恢复规则、语法错误计数、恢复提示、前端状态接口
- `pascal_s_lexer.l`
  - 词法错误计数、上限控制、提前终止状态
- `pascal_s_frontend.hpp`
  - 新增前端错误状态查询接口
- `pascal_s_driver.cpp`
  - 增加阶段性错误汇总输出
- `testing/error_recovery/*`
  - 新增错误恢复样例
- `testing/run_error_recovery_checks.sh`
  - 新增错误恢复回归脚本

## 5. 新增测试样例

新增目录：

- `testing/error_recovery/`

当前样例覆盖：

- `01_statement_and_call_recovery.pas`
  - 缺分号、`write/read` 参数恢复、调用实参恢复
- `02_formal_parameter_recovery.pas`
  - 形参列表恢复
- `03_index_recovery.pas`
  - 数组下标列表恢复
- `04_lexical_limit.pas`
  - 词法错误上限
- `05_unclosed_comment.pas`
  - 未闭合注释导致的提前终止

回归命令：

```bash
./testing/run_error_recovery_checks.sh
```

## 6. 当前能力边界

本次实现故意没有做以下内容：

- 自动补 token
- 智能拼写纠正
- 表达式内部复杂猜测恢复
- 带错继续生成目标代码

这不是缺陷，而是和课程设计目标一致的边界控制。

## 7. 实测现象总结

当前版本已经可以稳定做到：

1. 一份程序内连续报告多条语法错误
2. 在缺分号、坏参数列表、坏下标列表场景下继续向后分析
3. 词法错误过多时停止刷屏
4. 未闭合注释时只报词法阶段错误，不再额外制造伪语法错误
5. 正确程序编译链路不受影响
